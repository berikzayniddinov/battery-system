```markdown
---

# 🔋 Battery System – RUL Prediction

Этот проект состоит из **Backend (FastAPI + SQLite)** и **Frontend (HTML, CSS, JS)** для работы с данными батарей и прогнозирования остаточного ресурса (**Remaining Useful Life, RUL**) с системой аутентификации пользователей.

---

## 🚀 Запуск проекта

### 1. Клонировать репозиторий

```bash
git clone https://github.com/berikzayniddinov/battery-system.git
cd battery-system
```

---

### 2. Запуск Backend (FastAPI)

Перейдите в папку `backend`:

```bash
cd backend
```

Установите зависимости:

```bash
pip install -r requirements.txt
```

---

#### ⚙️ 2.1. Инициализация базы данных

**ВАЖНО:** При первом запуске или после обновления моделей необходимо инициализировать базу данных.

```bash
# Удалите старую базу данных (если существует)
rm battery_data.db        # Linux/Mac
del battery_data.db       # Windows

# Запустите скрипт инициализации
python init_db.py
```

После выполнения вы увидите:

```
🗑️  Удаление старых таблиц...
🔨 Создание новых таблиц...
✅ База данных успешно инициализирована!
✅ Созданы тестовые пользователи:
   - admin / admin123
   - testuser / test123
```

---

#### 📊 2.2. Загрузка датасета (опционально)

Если у вас есть CSV файл с данными батарей, загрузите его:

```bash
python load_dataset.py
```

После выполнения вы увидите:

```
✅ Загружено 15064 записей в таблицу battery_data для пользователя testuser
```

---

#### ⚡ 2.3. Запуск сервера

```bash
uvicorn main:app --reload
```

Backend будет доступен по адресу:
👉 [http://localhost:8000](http://localhost:8000)

Документация Swagger:
👉 [http://localhost:8000/docs](http://localhost:8000/docs)

---

### 3. Запуск Frontend

В новом терминале перейдите в папку `frontend`:

```bash
cd frontend
```

Запустите локальный сервер:

```bash
python -m http.server 8080
```

Frontend будет доступен по адресу:
👉 [http://localhost:8080](http://localhost:8080)

---

## 📂 Структура проекта

```
battery-system/
│
├── backend/
│   ├── main.py                # Основной FastAPI сервер
│   ├── auth.py                # Аутентификация и авторизация
│   ├── models.py              # SQLAlchemy модели (User, BatteryData, PredictionResult)
│   ├── schemas.py             # Pydantic схемы для валидации
│   ├── database.py            # Подключение к базе данных
│   ├── config.py              # Конфигурация (секретные ключи, настройки)
│   ├── ml_model.py            # ML модель для предсказания RUL
│   ├── init_db.py             # Скрипт инициализации базы данных
│   ├── load_dataset.py        # Скрипт загрузки CSV данных
│   ├── battery_data.db        # SQLite база данных (создается автоматически)
│   └── requirements.txt       # Python зависимости
│
├── frontend/
│   ├── index.html             # Главная страница
│   ├── script.js              # Логика отображения данных
│   └── style.css              # Стили интерфейса
│
└── README.md
```

---

## 🔐 API Endpoints

### Аутентификация

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/auth/register` | Регистрация нового пользователя |
| POST | `/api/auth/login` | Вход и получение JWT токена |

### Данные батарей (требуется авторизация)

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/battery-data` | Добавление новых данных батареи |
| GET | `/api/predict-rul/{battery_id}` | Получение предсказания RUL |

### Система

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/health` | Проверка состояния сервера |

---

## 🧪 Тестирование API (Postman)

### 1. Регистрация пользователя

**POST** `http://localhost:8000/api/auth/register`

```json
{
    "email": "user@example.com",
    "username": "testuser",
    "password": "password123"
}
```

### 2. Вход в систему

**POST** `http://localhost:8000/api/auth/login`

```json
{
    "username": "testuser",
    "password": "password123"
}
```

Ответ:
```json
{
    "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "token_type": "bearer"
}
```

**Сохраните `access_token` для следующих запросов!**

### 3. Добавление данных батареи

**POST** `http://localhost:8000/api/battery-data`

**Headers:**
- `Authorization: Bearer YOUR_ACCESS_TOKEN`

**Body:**
```json
{
    "voltage": 3.7,
    "current": 2.5,
    "temperature": 25.0,
    "capacity": 2500.0,
    "cycle_number": 1,
    "battery_id": "BAT001"
}
```

### 4. Получение предсказания RUL

**GET** `http://localhost:8000/api/predict-rul/BAT001`

**Headers:**
- `Authorization: Bearer YOUR_ACCESS_TOKEN`

Ответ:
```json
{
    "battery_id": "BAT001",
    "rul": 996.5,
    "confidence": 0.85
}
```

---

## 🧠 Модель предсказания RUL

Система использует **Random Forest Regressor** для предсказания остаточного ресурса батареи на основе:

- Текущего напряжения (Voltage)
- Тока (Current)
- Температуры (Temperature)
- Емкости (Capacity)
- Количества циклов (Cycle Number)

**Признаки модели:**
- Тренд деградации емкости
- Статистические показатели (среднее, стандартное отклонение)
- Параметры последнего измерения

Модель автоматически переобучается при добавлении новых данных.

---

## ✅ Требования

- Python **3.9+**
- SQLite (встроен в Python)
- Установленные зависимости из `requirements.txt`

---

## 🛠️ Зависимости

```txt
fastapi==0.104.1
uvicorn==0.24.0
sqlalchemy==2.0.23
scikit-learn==1.3.2
pandas==2.1.3
numpy==1.24.3
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
pymysql
```

---

## 🔧 Решение проблем

### Проблема: `ModuleNotFoundError: No module named 'backend'`

**Решение:** Убедитесь, что вы находитесь в папке `backend` при запуске сервера.

### Проблема: `sqlite3.OperationalError: no such column`

**Решение:** Удалите базу данных и пересоздайте её:

```bash
del battery_data.db
python init_db.py
uvicorn main:app --reload
```

### Проблема: `NOT NULL constraint failed: users.id`

**Решение:** Проверьте, что в `models.py` используется `Integer` с `autoincrement=True`.

### Проблема: Файл базы данных используется другим процессом

**Решение:** Остановите сервер (Ctrl+C), подождите несколько секунд, затем удалите файл.

---

## 📝 Заметки для разработчиков

- Пароли хешируются с использованием **SHA256** (для production рекомендуется использовать bcrypt или argon2)
- JWT токены действительны **24 часа**
- База данных SQLite подходит для разработки, для production используйте PostgreSQL/MySQL

---

## 📄 Лицензия

MIT License

---

## 👨‍💻 Автор

Berik Zayniddinov

---

## 🤝 Вклад

Pull requests приветствуются! Для больших изменений сначала откройте issue для обсуждения.

---
```

Теперь README содержит:
- ✅ Четкие инструкции по инициализации базы данных
- ✅ Описание всех API endpoints
- ✅ Примеры запросов для Postman
- ✅ Описание ML модели
- ✅ Раздел решения проблем
- ✅ Полную структуру проекта